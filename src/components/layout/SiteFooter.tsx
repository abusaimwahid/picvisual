import Link from "next/link";
import { BrandLogo } from "@/components/ui/BrandLogo";
import type { PublicBrandSettings } from "@/lib/brand/settings";
import type { PublicNavigationItem } from "@/lib/public/readers";
export function SiteFooter({ brand, navigation, settings }: { brand?: PublicBrandSettings; navigation: readonly PublicNavigationItem[]; settings: { name: string; email: string; footerText: string; socialLinks: string; copyright: string; phone: string; location: string } }) {
  const social = settings.socialLinks.split("\n").filter(Boolean).map((line) => line.split("|").map((part) => part.trim()));
  return <footer className="footer"><div className="footer-top"><div><span className="eyebrow">PICVISUAL / POST-PRODUCTION</span><p>{settings.footerText}</p></div><a className="footer-email" href={`mailto:${settings.email}`}>{settings.email} <i>↗</i></a></div><div className="footer-links">{navigation.map((item) => <Link key={item.href} href={item.href} target={item.openInNewTab ? "_blank" : undefined} rel={item.openInNewTab ? "noopener noreferrer" : undefined}>{item.label}<i>↗</i></Link>)}<Link href="/privacy">Privacy<i>↗</i></Link></div>{social.length > 0 && <div className="footer-social">{social.map(([label, url]) => <a key={url} href={url} target="_blank" rel="noopener noreferrer">{label} ↗</a>)}</div>}{settings.phone && <p><a href={`tel:${settings.phone.replace(/[^+\d]/g, "")}`}>{settings.phone}</a></p>}{settings.location && <p>{settings.location}</p>}<div className="footer-mark"><BrandLogo className="footer-logo-image" source={brand?.mainLogo} /></div><div className="footer-bottom"><span>© {new Date().getFullYear()} {settings.copyright || settings.name}</span><span>Built around the image.</span></div></footer>;
}
