import { createUser, updateUser } from "@/app/admin/actions";
import { StatusBadge } from "./AdminPrimitives";
type UserRow = { id: string; name: string | null; email: string; role: "OWNER" | "ADMIN" | "EDITOR"; isActive: boolean; createdAt: Date; lastLoginAt: Date | null };
export function UserManagement({ users, actorRole }: { users: UserRow[]; actorRole: string }) { return <div className="admin-users"><section className="admin-card"><h2>Create user</h2><form action={createUser} className="admin-user-form"><label>Name<input name="name" /></label><label>Email<input name="email" type="email" required /></label><label>Temporary password<input name="password" type="password" minLength={12} required /></label><label>Role<select name="role" defaultValue="EDITOR"><option value="EDITOR">Editor</option><option value="ADMIN">Admin</option>{actorRole === "OWNER" && <option value="OWNER">Owner</option>}</select></label><button type="submit">Create user</button></form></section><section className="admin-card admin-table-wrap"><h2>Users</h2><table><thead><tr><th>User</th><th>Role</th><th>Status</th><th>Last sign-in</th><th>Access</th></tr></thead><tbody>{users.map((user) => <tr key={user.id}><td><strong>{user.name || "—"}</strong><small>{user.email}</small></td><td><StatusBadge value={user.role} /></td><td><StatusBadge value={user.isActive ? "ACTIVE" : "INACTIVE"} /></td><td>{user.lastLoginAt ? user.lastLoginAt.toLocaleDateString() : "Never"}</td><td><form action={updateUser} className="user-action"><input type="hidden" name="id" value={user.id} /><select name="role" defaultValue={user.role}><option value="EDITOR">Editor</option><option value="ADMIN">Admin</option><option value="OWNER">Owner</option></select><input type="hidden" name="isActive" value={String(!user.isActive)} /><button type="submit">{user.isActive ? "Deactivate" : "Activate"}</button></form></td></tr>)}</tbody></table></section></div>; }
