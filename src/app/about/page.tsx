import type { Metadata } from "next";
import Link from "next/link";
import { SiteChrome } from "@/components/layout/SiteChrome";
import { getPublicPageContent } from "@/lib/public/readers";
export const metadata: Metadata = { title: "Studio — PicVisual", description: "A post-production partner built around visual craft, consistency and collaboration." };
export default async function AboutPage() { const page = (await getPublicPageContent("about")).data; const body = page?.sections.find((section) => section.type === "richText")?.content as { body?: string } | undefined; return <SiteChrome><main id="main" className="inner-page"><section className="page-intro"><span className="eyebrow">STUDIO / PICVISUAL</span><h1>{page?.title ?? "A post-production team built around visual craft."}</h1><p>{body?.body ?? "PicVisual partners with brands, e-commerce teams, photographers, agencies and studios to turn commercial captures into a coherent visual world."}</p></section><section className="about-statement"><div className="about-image"><span>COMPOSITION / CRAFT / CONSISTENCY</span><div /></div><div><span className="section-number">OUR APPROACH</span><h2>Make the standard<br />visible in every frame.</h2><p>The strongest production workflows balance care with clarity. We establish a finish, work from a shared brief, and carry that decision-making through every deliverable.</p><p>That means a more consistent experience for the teams who create, review and eventually publish the work.</p><Link className="text-link dark" href="/contact">Work with PicVisual <i>↗</i></Link></div></section></main></SiteChrome>; }
