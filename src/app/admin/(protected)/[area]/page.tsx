import { EmptyState, PageHeader } from "@/components/admin/AdminPrimitives";
import { sectionRegistry } from "@/cms/section-registry";
const labels: Record<string, string> = { homepage: "Homepage", pages: "Pages", portfolio: "Portfolio", services: "Services", media: "Media", testimonials: "Testimonials", clients: "Clients", faq: "FAQ", navigation: "Navigation", footer: "Footer", enquiries: "Enquiries", seo: "SEO", settings: "Site settings", activity: "Activity" };
export default async function AdminFoundationPage({ params }: { params: Promise<{ area: string }> }) { const { area } = await params; const title = labels[area] ?? "Admin"; const isHomepage = area === "homepage"; return <section className="admin-content"><PageHeader eyebrow="CMS FOUNDATION" title={title} description={isHomepage ? "The supported section library is registered and validated; visual editing is the next CMS phase." : "The database model and permission boundary are ready; complete CRUD tooling is the next CMS phase."} />{isHomepage ? <div className="admin-card"><h2>Supported section types</h2><div className="section-library">{sectionRegistry.map((section) => <article key={section.type}><strong>{section.label}</strong><p>{section.description}</p><small>{section.editor} editor</small></article>)}</div></div> : <EmptyState title={`${title} management is next`} description="This intentionally has no simulated data or fake controls. The schema, validation, and protected application shell are ready for the next editor implementation phase." />}</section>; }
